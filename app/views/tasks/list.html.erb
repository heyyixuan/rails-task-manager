<h1>My Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, show_task_path(task) %>
      <%= link_to "🗑", destroy_task_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
    </li>
  <% end %>
</ul>
<p><%= link_to "Add a new task", new_task_path %></p>
